git init 

git clone <url of my repo>

git add . 
    //or use git add <file name> 

git commit -m "message" 
  //or instead of using add then commit use 
      git commit -am "message"

git push 
   //enter my account name and password 
      or by using git config --global user.email <email> 
            git config --global user.name <username>